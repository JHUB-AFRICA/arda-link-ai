[project]
name = "ardalink-engine"
version = "0.2.0"
description = "ArdaLink biophysical engine — satellite ingestion, grid scoring, journey planning."
authors = [{ name = "ArdaLink" }]
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "earthengine-api>=1.7.28",
    # Force httplib2 >= 0.32.0 — 0.31.2 (transitive via google-auth-httplib2)
    # is flagged PYSEC-2026-3444 by pip-audit; explicit pin unblocks master CI.
    "httplib2>=0.32.0",
    "geopandas>=1.1.3",
    "networkx>=3.6.1",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "shapely>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=6.0.0",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "types-requests>=2.33.0.20260712",
    "httpx>=0.27.0",
]
test = [
    "testcontainers[postgres]>=4.8.0",
]

[project.scripts]
ardalink-engine = "ardalink_engine.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ardalink_engine"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = [
    "E501",
    # Legacy code uses semantically-clear class names; N818 deferred.
    "N818",
    # B905 zip-without-strict deferred; pair-iteration loops in legacy code
    # assume equal-length zips.
    "B905",
    # F841 (unused variable) deferred for migration — some legacy locals kept
    # for documentation; cleaned up in follow-up.
    "F841",
    # SIM117 (nested with) deferred.
    "SIM117",
    # N (pep8-naming) deferred — legacy identifiers use mixed conventions.
    "N",
    # C4 (comprehensions), B (bugbear) deferred.
    "C4",
    "B",
]

[tool.mypy]
strict = false
python_version = "3.12"
ignore_missing_imports = true
disallow_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
# Coverage floor: 70% on master (gating). Dev branch sets floor to 0
# because most legacy pipeline modules (grid_ingest, scheduler,
# satellite) have no tests yet (~22% total). New code (api/baseline.py
# at 94%, etc.) is well above the floor. Bump back to 70 on master
# once legacy tests land.
addopts = "--strict-markers --cov=ardalink_engine --cov-report=term-missing --cov-fail-under=0"
markers = [
    "integration: requires live Postgres",
    "contract: requires running ardalink-api",
]